Resources:
  CurrencyStandardizer:
    Type: AWS::Serverless::Function
    Properties:
      Handler: lambda_function.lambda_handler
      Runtime: python3.10
      CodeUri: .
      MemorySize: 512  # Increase the memory size if needed (e.g., 512 MB or 1024 MB)
      Timeout: 30

